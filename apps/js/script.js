const ubuntu = document.getElementById("ubuntu").innerText;
const debian = document.getElementById("debian").innerText;
const arch = document.getElementById("arch").innerText;
const manjaro = document.getElementById("manjaro").innerText;
const fedora = document.getElementById("fedora").innerText;
const mint = document.getElementById("mint").innerText;
const kali = document.getElementById("kali").innerText;

function copyubuntu(){
  navigator.clipboard.writeText(ubuntu)
  alert("Code is copy!")
}
function copydebian(){
  navigator.clipboard.writeText(ubuntu)
  alert("Code is copy!")
}
function copyarch(){
  navigator.clipboard.writeText(ubuntu)
  alert("Code is copy!")
}
function copymanjaro(){
  navigator.clipboard.writeText(ubuntu)
  alert("Code is copy!")
}
function copyfedora(){
  navigator.clipboard.writeText(ubuntu)
  alert("Code is copy!")
}
function copymint(){
  navigator.clipboard.writeText(ubuntu)
  alert("Code is copy!")
}
function copykali(){
  navigator.clipboard.writeText(ubuntu)
  alert("Code is copy!")
}
